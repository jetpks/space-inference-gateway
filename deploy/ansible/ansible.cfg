[defaults]
# A failed task otherwise discards queued handlers: a run that copies a
# config then dies later leaves the service running stale config forever.
# See repos/config-mgmt-base/ansible.cfg for the originating incident note.
force_handlers = True
roles_path = roles
